// Configuraciones iniciales 

// Una ves instalado mongo debemos crear una carpeta contenedora donde iniciar mongo, por ejemplo una carpeta llamada base. Luego debemos situarnos donde esta esa carpeta sin entrar en ella abrir una terminal en esa ubicación y escribir el siguiente comando # mongod --dbpath ./base o el nombre de la carpeta que le pusieron. 

// Dejar esa consola ejecutada e iniciar otra consola dentro de la carpeta donde iniciamos anteriormente y ejecutamos el comando mongo 